{% trans_default_domain 'FOSUserBundle' %}

<form novalidate="novalidate" action="{{ path('fos_user_group_new') }}" {{ form_enctype(form) }} method="POST" class="fos_user_group_new">
    {{ form_widget(form) }}
    <div>
        <button type="submit" class="btn btn-default">{{ 'group.new.submit'|trans }}</button>
    </div>
</form>
