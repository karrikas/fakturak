{% trans_default_domain 'FOSUserBundle' %}

<form novalidate="novalidate" action="{{ path('fos_user_group_edit', {'groupName': group_name} ) }}" {{ form_enctype(form) }} method="POST" class="fos_user_group_edit">
    {{ form_widget(form) }}
    <div>
        <button type="submit" class="btn btn-default">{{ 'group.edit.submit'|trans }}</button>
    </div>
</form>
